services:
  back:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: hamari-backend-local

  front:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_BASE: ""
    image: hamari-frontend-local
